title: ""
languages: 'ja'
default_lang: 'ja'
defaults:
  -
  scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
   values:
      layout: "project" #"new" #"default"
      category: "project" #"new" #"default"
  -
  scope:
      path: "./wikiref.md" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "page2" #"default" #"project"
      category: "default"  #"default" # "project"
      
