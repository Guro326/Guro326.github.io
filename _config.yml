title: 
languages: 'ja'
default_lang: 'ja'
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "project"
      category: "project"
